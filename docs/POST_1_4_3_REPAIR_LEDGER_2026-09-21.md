# Veyra 1.4.3 之后修复总账

日期：2026-09-21
范围：公开标签 `v1.4.3`（`3b4570e`）之后，到当前分支
`codex/fg-stability-20260921` 的 `0de0a1d`，并单独标出工作区未提交内容。

这份文档是工程总账，不是新的发布说明。它把真正进入产品的修复、只用于
诊断的改动、已经撤回的实验和仍未解决的问题分开。正式 GitHub 最新版仍是
1.4.3；1.4.4 目前只有本地 beta 包。

## 结论

1.4.3 之后已经完成并保留的主要工作集中在采集颜色/HDR、字幕与全屏操作、
输出限帧、NR 时间域防闪烁、设置与界面回归、以及补帧诊断。补帧模块没有被
“修好到所有负载稳定满 4X/6X”：真 1080p→4K 超分加 NR 时，DLSS 4X/6X
仍有长间隔，XeSS 4X 的重负载连续性比 1.4.3 更差；这两个结论都以实测为准。
RTX Video HDR 的核心功能和 FSR4.1 回退决定属于更早阶段；本时间段没有重新
引入 FSR4.1，也没有改写 RTX Video HDR 的核心算法，新增的是采集卡颜色合同和
Elgato 专用元数据路径。

## 已进入当前产品分支的修复

| 范围 | 主要改动 | 状态与边界 | 代码/证据 |
| --- | --- | --- | --- |
| 采集颜色 | 采集卡增加输入色彩空间选择：自动、PQ、HLG、Rec.709；增加自动/有限/完整范围；按设备路径保存，兼容旧配置；P010/P016 才允许手动 HDR 覆盖；修复色轮跨红色色相零点和分区响应空洞 | 单元、GPU 和 UI 合同通过；真实设备仍需按型号复测 | `275f561`；`RELEASE_NOTES_1.4.4.md` |
| Elgato 4K60 Pro MK.2 | 读取驱动私有 HDR InfoFrame，原生 HDR 时关闭卡内 HDR→SDR 映射；停止采集时恢复可读的原状态；失败时记录诊断，不改全局色彩公式 | 只针对 MK.2 + P010；代码/模拟合同通过，未在本机接到该卡做最终实卡验收 | `be3e2e6`；`ELGATO_MK2_HDR_PLAN_2026-09-20.md` |
| 采集时间线 | 对原生未压缩帧过滤“持续粘住”的驱动 discontinuity 标志，但要求连续时间戳和到达节奏；孤立标志、压缩输入、真正断点仍保留 | 解决的是已证实的历史重置问题，不保证驱动时间元数据永远正确 | `ed00218`；`CAPTURE_STICKY_DISCONTINUITY_2026-09-20.md` |
| 输出节奏 | 增加关闭/跟随显示器/自定义 FPS 三种输出限帧；最终默认是关闭；限帧只选择呈现候选，不改媒体时钟、补帧倍率或源输入 | XeSS/FSR 由提供方持有交换链，当前明确显示“不支持独立输出限帧”，不会偷偷压低输入帧率 | `6404eda`、`dcf1d9e`、`dd69e1f`；`CORRECTIVE_AUDIT_2026-09-21.md` |
| 字幕与全屏 | 字幕默认保留字号，双行不再自动缩小；行数/适配选项可保存；全屏锁定按钮和快捷键，锁定时鼠标活动不弹控制条，Esc/F11 仍可退出 | 文本布局回归通过；位图字幕保留原始几何；HDR 浮窗颜色问题没有被宣称解决 | `6404eda`、`dd69e1f` |
| 设置与 NR 页面 | 增强页改为顺序布局，修复新增控件后的错位/空白块/滚动重绘问题；恢复“NR 增强”术语；保留现有 0/1/2 三个真实模型档 | 003/033 只作为参数映射和参考，不是新增模型 ID；快速滚动、真实多屏仍不是全面视觉验收 | `dd69e1f`、`dcf1d9e`；`CORRECTIVE_AUDIT_2026-09-21.md` |
| NR 时间域防闪烁 | 新增默认关闭的时间域残差稳定：双历史纹理、光流重投影、邻域一致性、保护区/羽化、无效时间和切镜 reset；保留 HDR 有符号分量；复用 GPU 邻域工作降低重复开销 | 合成 GPU/指纹回归通过；自然样本稳定源残差波动下降约 71–76%；不是独立输入降噪，快速游戏鬼影/主观画质未完全验收 | `6404eda`、`ab7979c`；`NR_QUALITY_PERFORMANCE_ACCEPTANCE_2026-09-21.md` |
| NR 预设 | 新增可命名预设的保存/覆盖、应用、删除；预设只覆盖 NR 模型、残差、剔除区和时间域开关，不碰 SR/FG/颜色/音频/采集 | 预设/UI 回归通过 | `73795ef`；`NR_FG_FOLLOWUP_ACCEPTANCE_2026-09-21.md` |
| 补帧开关与历史 | 移除文件播放中按媒体迟到自动 suppress/resume XeSS 的隐式门；保留用户选择的倍率和有界预览跳过；修正采集未同步时的呈现节奏计算 | 这是取消“软件偷偷降档”的修复，不等于高负载 XeSS 已稳定；重负载真 SR 连续性仍开放问题 | `dd69e1f`；`CORRECTIVE_AUDIT_2026-09-21.md` |
| 统计与诊断 | Present 入口/返回分开采样，附带 decode/process/ready/queue/PTS lineage；避免把过期采样或 Present 等待重复算成 GPU 计算；补充 FG/XeSS timeline、fence、deadline 和 provider output 诊断 | 这些数据用于归因，不是屏幕到人眼延迟，也不是性能提升本身 | `f029707` 及后续诊断提交；`NR_FG_FOLLOWUP_ACCEPTANCE_2026-09-21.md` |

## 交付和测试层面的修复

- 修正了早期手工 1.4.4 beta 包漏带 FFmpeg/dav1d DLL 的打包错误，并重新做了
  便携包启动、依赖隔离、manifest 大小/SHA-256 和禁止文件扫描。
- 最新保留的本地包是：
  `E:/项目/Veyra/test-packages/1.4.4beta-20260921-nr-followup/`，ZIP SHA-256
  为 `A035A8C1956E1C539D39FEC917A273692A685B0A2184A026196700268B453ACB`，
  对应源码 checkpoint `f029707`。后续 XeSS/调度诊断没有重新打包进该 ZIP；
  这是本地 beta，不是 GitHub Release。
- 最新包的便携 smoke 7/7、后端切换/失败回退、UI 合同、字幕、时间域 shader
  和 124 个载荷的哈希检查通过。RTX 5070 的通过结果不能替代 30/40 系、真实
  GC551/GC573、第二显示器或物理屏幕延迟验收。

## 只用于诊断或研究，不能算“已修复”

这些提交增加了证据和分析能力，但没有把结果伪装成产品性能修复：

- XeSS 源 PTS → `frameRenderTime` 的候选入口，仅由
  `VEYRA_TEST_XESS_SOURCE_TIMING=1` 启用，默认关闭；重负载源吞吐有改善，仍有
  短长突发，尚未通过均匀 4X 和图像对应验收。
- XeSS provider fence/deadline 只读追踪，用来区分资源未就绪、截止时间等待和
  SDK 内部呈现；不能据此删除 fence 或声称找到了唯一根因。
- Magpie experimental 固定提交源码审查，记录了 presenter owner、XeLL、时间戳
  和交换链边界；没有复制 Magpie 二进制或运行库。
- 1.4.3 与当前版本的同条件矩阵：原生输入 DLSS 约相当，真 SR DLSS 4X/6X
  仍失败均匀节奏；真 SR XeSS 4X 当前源连续性低于 1.4.3，但旧版通过频繁
  suppress/resume 偷停补帧，不能把旧版更高源速率当成成功。

## 已尝试并撤回，今后不要重复

以下方向已有 A/B 反证或会破坏产品契约，已从当前方案删除；原始数据仍保留在
`FG_EXPERIMENT_INDEX_2026-09-21.md` 和对应实验记录中：

- 同组 GPU 成本估算器、去掉 Present 清屏、光流历史复制、原生光流尺寸直传 DLSSG；
- 提高图形队列优先级、XeSS 只保留一组 pending 源帧、NVOF 与 Video SR 重叠；
- 全局取消 DLSS admission、无条件接纳晚到生成、固定等待和盲目增加队列；
- 简单 CPU/presenter 拆线程、拆 owner、逐输出发布和未经新证据支持的
  `FG(B)`/`Enhance(C)` 并行重叠。

另外两项曾短暂加入、后来明确回退：

- 用已接受 Present 间隔作为 XeSS `frameRenderTime`：该间隔混入了 provider 等待，
  已恢复为未知时传 0；
- 采集设置事务等待首帧的固定 2 秒超时：会把慢设备误判为错误，已撤回，保留
  原有恢复路径。

## 目前仍未解决

1. 真 1080p→4K 超分 + NR 下 DLSS 4X/6X 的均匀呈现和长间隔。
2. 真 SR + NR 下 XeSS 4X 的连续性、功耗波动和用户所说的果冻观感。
3. 独立输入降噪；时间域防闪烁不等于源画面去噪。
4. XeSS/FSR 提供方交换链的最终输出限帧。
5. 杜比视界 P5/RPU 重建（按用户决定暂缓）。
6. GC551/GC573、Elgato 实卡和多显示器物理验收，以及 input-to-photon 延迟。
7. NVIDIA FSR4.1 实验仍保持回退，本时间段没有重新接入。

## 当前 Git 状态

当前分支上的产品代码基线仍是上述提交；工作区还有一组未提交的实验清理：
删除已失败的全局 admission A/B、Video SR overlap 环境入口和 overload-baseline
测试选项，并同步更新实验索引和运行方案。它们尚未进入 beta 包、尚未合并 main、
尚未推送或发布。不要把工作区未提交内容当成已发布修复。

## 提交范围索引

产品代码修复提交：`275f561`、`be3e2e6`、`ed00218`、`6404eda`、`73795ef`、
`dcf1d9e`、`dd69e1f`、`ab7979c`、`f029707`。

发布/打包/回归记录提交：`05998da`、`7618072`、`c84eb72`、`8285153`、
`8bc4c0b`、`8006d21`、`8e25f6a`、`e71d715`。

补帧诊断、实验边界和方案提交：`d2ae8ee`、`0143baf`、`f049a13`、`d76577d`、
`512de1c`、`a0e39ea`、`53490a6`、`38f00da`、`d5e7ed2`、`b69d22c`、
`4eff0e8`、`0de0a1d`。这些提交中只有明确标为产品修复的部分进入上面的“已进入
当前产品分支”清单，其余是证据或已撤回实验。

## 证据入口

- [1.4.3 正式版说明](RELEASE_NOTES_1.4.3.md)
- [1.4.4beta 说明](RELEASE_NOTES_1.4.4.md)
- [NR/FG 本地验收](NR_FG_FOLLOWUP_ACCEPTANCE_2026-09-21.md)
- [补帧实验索引](FG_EXPERIMENT_INDEX_2026-09-21.md)
- [Magpie 调度源码审查](MAGPIE_SCHEDULING_SOURCE_AUDIT_2026-09-21.md)
- [当前状态](CURRENT_STATUS.md)
