# 全软件已知问题台账与修复路线

日期：2026-09-22。基线 `edfd886`，`codex/5090-capture-fg-20260921`。
工作区 `E:/项目/Veyra/worktrees/playback-nr-20260920`。
本轮为历史证据、现有源码及验收记录复核，只更新文档，不实施产品修改。

用户要求覆盖整个软件。历史 XeSS 和 DLSS 6X 是本台账的核心未解决项，
不能被最近一份采集日志取代，也不能因已有多轮“修复完成”记录而关闭。
本台账是已知问题及验收缺口清单，不是全源码逐行审计或所有潜在 bug 已穷尽的证明。
具体输入/资源/HDR/清晰度施工边界见[全链路方案](FULL_CHAIN_REGRESSION_REPAIR_PLAN_2026-09-22.md)。

## 1. 状态口径

- **未解决**：用户症状或测量问题仍存在，不能承诺已定因。
- **代码缺口**：当前实现已确认缺少合同或功能，但不代表已证明某次用户故障由此引起。
- **候选未验收**：已有实验/未提交改动，没有达到交付条件。
- **已修待回归/实机**：已有修复和限定证据；不重复施工，相关路径变更后回归。
- **暂缓/撤回**：遵守用户决定，不转为本轮施工项。

P1 表示影响主要使用体验，P2 表示功能缺口或限定工况风险；不是给未知根因预判严重性。
下面的历史测试不是今天对 HEAD 的重新跑分。版本、素材尺寸和统计范围不同的成绩不混用。

## 2. DLSS：6X 历史问题没有解决，2X 也不能漏

### D1 / P1：高提交率但呈现不均匀

1.4.3 发布说明本身就写明固定 6X 尚未均匀呈现：原生 4K60、NR 内部 1080、
SR 旁路时，部分近期窗口约 295–303 次提交/秒，仍有约 16.8ms 长间隔。
这不是稳定 360fps，更不是同条件真实超分成绩。

后续同条件、同机器的 30 秒版本对照为：

| 场景 | 1.4.3 提交/s | 当时被测开发版提交/s | 间隔 P99，旧/开发版 |
| --- | ---: | ---: | --- |
| 原生 4K、NR、DLSS 6X | 272.03 | 268.23 | 16.934 / 16.899ms |
| 真 1080p→4K 超分、NR、DLSS 4X | 135.44 | 134.42 | 17.063 / 17.099ms |
| 真 1080p→4K 超分、NR、DLSS 6X | 139.67 | 138.05 | 17.125 / 17.127ms |

原生输入的后段 298 与另一轮全段 268/272 不能拼成“性能退步 30fps”的结论。
但三组结果都不能证明均匀高倍率。60Hz 源帧间隔 16.67ms 正常；
若目标是 360 次均匀输出，输出间隔应接近 2.78ms，频繁出现整段 16.8ms 空档就不正常。
P99 不能单独说明全部观感，还要核对空档频率、真实画面时间和实际显示。

已有证据：一组重负载 DLSS 4X/6X 样本约 132.85/135.93 提交/s，
295/361 组 real-only 与预算拒绝/历史播种相关，生成后过期为 0。
另有完整组 GPU 测量：原生 NR 6X 约 18.07ms、真实超分+NR+6X 约 22.92ms。
这些是当前实现下的串行工作耗时，不是不可优化的硬件下限；
也不能把所有亏损说成“算好了但故意不显示”。

下一步不是再次全局取消预算：

1. 利用已有逐组证据，把长空档分别落到输入缺失、处理晚到、预算拒绝、一次播种、
   生成无效、已就绪未及时呈现。每组只按真实经过计数，防止重复归因。
2. 核对单次恢复是否在连续源上被重复触发、预算是否使用同组正确的 ready fence/期限、
   原帧早呈现是否错误影响下一组状态。已有正确播种修复保留；没有反例就不重写它。
3. 对“生成已有效且及时就绪但漏呈现”只修其具体顺序/状态错误；
   对 GPU 关键路径超预算的组，不用推迟期限、堆帧或减画质制造通过。
4. 使用带已知运动位置的画面核对原帧和各生成帧的时间位置，补齐旧记录留下的
   interpolation-position 验收缺口。纹理像素未被覆盖不等于插值时间正确。

完成门槛：同工况全段长空档和突发比例改善，源覆盖、帧龄、画质不退步；
只有平均提交率上升不接受。不能承诺在当前计算预算下所有机器满 6X。

### D2 / P1：5090 连 2X 不满、低占用

4K P010/60、原生 NR、质量光流的用户日志显示源处理约 56–58fps、
总提交约 106–114fps；预算约 999 接受/1 拒绝，690 有效生成/0 无效，
689 生成呈现/1 过期。质量光流约 5.3ms、NR 约 8.5ms，ready 约 14.7–15.3ms。
因此这份日志不支持“大量生成帧被门禁挡掉”的解释。

已接受 P010/P016 冗余 CPU 上传消除，本机上传中位值约 2.4→0.93–0.98ms，
18 个 GPU 像素指纹相同。仍缺 5090 对应整链路复验；切换重建约 1.618 秒、
约 95 丢源另算，不能混进稳态。
下一步沿输入→上传→光流/增强→生成→呈现找第一处超期，优先 2X 基础连续性，
再上 4/6X。不能因 5090 GPU 平均 60% 就断言全部功能应轻松跑满。

### D3 / P1：30/40 系兼容与持续运行

1.4.1 有用户反馈可用，不能推翻这段历史；但“能创建 Feature/能选 6X”
和“当前版本长时间在所有 30/40 卡均匀 6X”是两项验收。
保留已有原生/兼容分支，50 系不套 30/40 补丁；针对新改动核对切换、恢复、
超时与退出。当前没有新实卡证据，不能重新标为普遍打不开，也不能标全型号通过。

## 3. XeSS：4X 连续性倒退与果冻/闪烁分开处理

### X1 / P1：重负载连续性确实比 1.4.3 差

同一版本对照：原生 4K+NR+XeSS 4X 两版均约 60 源帧/s；
真 1080p→4K 超分+NR+XeSS 4X，源帧率 58.51→39.84/s，
外层 Present 间隔 P99 30.279→40.921ms；SDK 输出约 96.39→129.79/s。
不能用输出数字上升掩盖源运动覆盖变差。

旧版每 30 秒反复 suppress/resume 27/37 次，部分时间实际没有补帧。
当时比较基线的 XeSSPresenter/Pacing 实现与 1.4.3 相同，外围启停行为不同；
未做完整单提交二分，因此不能说已证明唯一致因提交，也不能恢复偷偷停补帧来冒充修好。
XeSS 最高 4X，没有 6X。XeSS 当时交换链为 2560×1440，DLSS 生成 4K，
这些数据只支持同后端版本比较，不支持两算法性能排名。

### X2 / P1：provider 等待与 GPU 工作交织，低功耗不是空闲证明

同工况旧测：XeSS 2X 源 60、输出 120/s、GPU 91.58%；4X 源 39.84、
输出 129.79/s、GPU 72.73%。诊断关闭 pacing 可到源 51.98、输出 183.61/s、
GPU 94.81%，但突发呈现未通过，不能作为发布方案。

351/351 个保留的首子帧调度入口仍有未完成 fence；入口到 deadline 段
平均 8.465ms/P95 14.300ms，随后等待约 3.815ms。约 8.5ms 包含 GPU 依赖、
队列与唤醒，不能全删，也不能全算 GPU 算法耗时。
另一个 SDK 返回 47.473ms 的样本内含 36.954ms native Present。

未完成的下一步是对具体等待归属做闭环：将该 fence 的 signal 队列、前序提交、
就绪时刻、实际呈现唤醒关联起来，区分图未完成、提供方生成未完成、系统 Present 阻塞、
就绪后调度迟到。没有找到确切错误前，不再做简单拆 owner、换队列或增加并行的实验。

### X3 / P1：已有源时间候选，不是已接受的优化

`VEYRA_TEST_XESS_SOURCE_TIMING` 默认关闭；历史源吞吐约 40→52/s，
持续样本约 52.09/s，但约四分之一输出间隔小于 1ms，偶发约 46ms 空档。
Intel 允许 frameRenderTime=0，不能把默认 0 当 API 违规。
只补这项候选尚缺的完整节奏和图像验收，不包装成新方案重跑吞吐数字；
若不能同时改善连续性、画质和帧龄，不启用。已失败的一组 pending 方案不重试。

### X4 / P1：果冻、拖动掉帧、欠速闪烁没有统一定因

切换能力缓存/XeLL 周期、窗口变换 motion 映射、断点恢复已有局部修复；
合成拖动/缩放回归通过不能证明快速游戏转身无果冻。
下一步固定 NR/SR/光流设置，对同一源对检查 motion、颜色、HDR 基底、映射纹理、
描述符和 provider 实际消费结束，核对欠速与恢复后的图像。
算法运动估计缺陷、颜色跳变、时间位置错乱分别记录；不默认换 AMD 光流，
不靠锐化或默认 NR 防闪滤镜掩盖，不在拖动时关闭生成。

## 4. 全软件问题与验收缺口总表

下表补充 D1–D3、X1–X4，既有修复不重新记成未修 bug。

| 编号/优先级 | 模块与问题 | 当前状态 | 后续动作与关闭条件 |
| --- | --- | --- | --- |
| C1/P1 | 原生采集间歇断供/卡死 | 未解决；最新日志效果全关仍有 1.18/1.31 秒回调间隔 | 查回调入口/退出、锁、驱动、owner 首处停顿；包含断档全程，不能拿恢复后快帧结案 |
| C2/P1 | 重开后变糊/选错模式风险 | 数字格式索引 URI 和默认重连 FPS 核对有代码缺口；用户变糊因果未证实 | 稳定格式键贯穿最近打开/协商/重连；枚举重排测试，不擅自降模式 |
| C3/P1 | 总体采集清晰度不如 PotPlayer | 未解决；已有 YUY2 1:1 合成亮度测试无额外模糊 | 同格式同尺寸先比较源/1:1，再看色度重建、缩放与 NR 残差；不能简单升锐度 |
| C4/P1 | GC573 RGB 53/57 而 OBS 60fps | 实卡根因/验收未完成；近期 FPS 统计修复不能证明吞吐修好 | 分请求/连接/回调/处理/提交；复用 C1 的边界证据，不伪造重复帧凑数 |
| A1/P1 | GC551 无声、部分多声道卡 | 缺时间戳、PCM/位流 sink、GetFormat 回退已有修复，型号实卡未验收 | 核对驱动音频 pin/载波/解码/输出；不按品牌绑定无关麦克风 |
| C5/P2 | 压缩采集丢包、B 帧/硬解恢复 | EAGAIN、输出身份、PTS、关键帧恢复已修；真实卡溢出和其他 codec 未全面验收 | 保留有界恢复；相关修改回归丢包/软硬切换，不把原生 P010 当压缩解码 |
| C6/P2 | 压缩 10bit/HDR 输入 | 功能边界：软件转换明确到 8bit NV12；非原生 P010 路径 | 单独设计保位深/HDR合同后才能支持，不能以现有原生 P010 支持冒充 |
| F1/P1 | 两后端欠速闪烁/间歇卡顿 | 未解决；不限定高倍率，也不认定精确 15 秒 | D/X 两后端从 2X 验起，分别查运动停顿、纹理/亮度交替和 HDR 输出切换 |
| H1/P1 | HDR 查询失败误作 SDR | 确认代码缺口；三态候选只构建未运行 | 查询失败、明确 SDR、换屏和新会话故障注入；成功缓存不能跨显示目标乱用 |
| H2/P2 | HDR 控制浮窗导致色调切换 | 全屏锁可减少触发，不是颜色链路修复 | 核对交换链/输出色彩空间/窗口合成；真实 HDR 显示器验收，不能用 SDR 截图结案 |
| H3/P2 | Elgato MK.2 HDR 偏色/自动识别 | 用户已反馈修好；私有元数据修复保留 | 不再全局调颜色；设备树、原状态恢复和非目标设备作为回归边界 |
| N1/P2 | 独立输入降噪 | 未交付；VFX 探针创建失败，无有效降噪输出或 GPU 互操作验收 | 先证明许可内运行时/API/模型可用，再单独接共享图；当前没有可承诺的实现方案 |
| N2/P2 | NR 时间域防闪的画质/成本 | 默认关闭；残差 shader 局部通过，快速运动主观验收不足 | 细节、鬼影、HDR、开关/导出回归；42ms 旧数据非新增滤镜/屏幕延迟 |
| N3/P2 | NR 风格 0/1/2 与 033 | 非新模型来源；预设不能冒充额外风格 | 保留真实三个模型档；不再造 3–6 ID，不把 NR 改名降噪 |
| P1/P2 | XeSS/FSR 自定义最终输出限帧 | 当前明确不支持，默认不限；需求未完成 | 必须在提供方最终输出层生效且不压源帧；先确认可用接口，不用输入限速代替 |
| P2/P2 | 原生 Reflex+FG、VRR 与撕裂 | Reflex+FG 回退低排队，VRR 未知；XeSS 已能传 VSync | 将队列节奏与扫描同步分开验收；完整原生标记/实屏证据不足时不宣称完成 |
| U1/P1 | UI 快滚重叠/拖影/控件闪烁/拖动跟不上 | 布局/重绘已修、静态和部分控件测试通过，快运动实屏覆盖不足 | 高 DPI、连续滚动、弹窗、拖动与 HDR 联动回归；有复现再修局部绘制 |
| U2/P1 | 边角缩放/跨屏卡死 | 1.4.3 已修嵌套回调/DPI，用户已验收可用 | 本轮资源/显示改动必须重新回归；合成 DPI 不冒充真实跨屏 |
| U3/P2 | 设置、命名预设、V 对比、单项还原 | 已有修复与限定回归，无新复发证据 | 旧配置迁移/重开持久化/取消事务回归，不当新功能重写 |
| S1/P2 | 字幕弹窗、连续调节、双行变小/图形字幕 | 已修；新默认不自动缩小，位图保留几何 | 多轨、ASS 显式定位、双语、PGS、多 DPI/全屏覆盖；未支持 codec 不冒充支持 |
| M1/P2 | MKV 打开慢/seek、音轨切换、文件时钟 | 既有硬解/媒体与静音时钟修复保留；无本轮复发证据 | 同大文件冷开/seek/多音轨和欠速音频连续性回归；不能凭旧“30秒”记录判当前仍失败 |
| E1/P2 | 导出失败/尾帧、调色不应用、NR+HDR 截图 | 已有 NVENC/CFR/颜色/截图修复；用户 HDR 实屏组合未全覆盖 | 共享图改变时跑短导出与取消/重开、音频、字幕、调色、截图；不恢复逐帧生产门禁 |
| R1/P2 | 窗口/桌面采集、PS5 串流 | 已有独立时钟/连接修复，无这次新故障证据 | 源切换/静止画面/最小化/重连与公共处理图回归；无实源则明确未测 |
| B1/P2 | 测试包、源码和诊断 staging 混用 | 曾有漏 DLL/旧 manifest，已修包不代表任意 staging 可交付 | 下次打包固定源码和组件清单，包含/排除修复逐项核验，不把编译通过当便携包通过 |
| Z1 | 杜比视界、NR 叠层、NVIDIA FSR4 | 前两者暂缓，FSR4 撤回 | 不自动恢复施工；现有 AMD FSR SR 的 HDR 不支持也要明确，不冒充通用 HDR 增强 |

当前源码复核定位：`VideoPresenter.cpp:171` 历史身份、`:177` 源时间候选、
`:272` 提供方限帧、`:284` FSR 同步、`:293` Reflex 回退；
`CaptureCompressedDecoder.cpp:96` 压缩高位深转换；`EnhanceGraph.cpp:804` FSR SR HDR 边界。
其余采集/资源位置见全链路方案第 3 节。行号对应本轮工作树，未来修改应按符号定位。

## 5. 施工优先级与禁止重复

1. **第一优先级是 D1/D2/X1–X4/F1。** 现有逐组数据先归类，不再先增加大堆日志。
   同时用 C1 全关断供作为对照，防止把公共链路停顿归给 FG；不能等所有采集/UI 问题
   都结束才处理历史 6X 和 XeSS。
2. **可独立关闭的确定性缺口是 C2/H1。** 以故障注入和合同回归为门槛，
   不宣称它们包办所有闪烁、卡死、变糊。C3 单独做匹配画质对照。
3. **接受任何调度修复前回归其他模块。** 低/高倍率、两后端、原生与真超分、
   SDR/HDR、文件与采集分开；相关生命周期覆盖切换/seek/暂停/拖动/跨屏/退出。
   公共图改动再验截图/导出/字幕/颜色，不能只跑 FG smoke。
4. **功能缺口单列。** 独立降噪、提供方输出限帧、完整 Reflex 有实现前提，
   不作为这轮卡顿根因修复的假承诺。暂缓项维持暂缓。

不再安排：全局取消 admission、扩队列、固定延迟、提高队列优先级、简单拆线程/
owner/逐输出发布、无新关键路径证据的 FG/Enhance 重叠、NVOF/SR overlap、
同组成本估算器、去清屏/光流历史复制/原生光流尺寸直传、XeSS 一组 pending、
Present 间隔反喂 provider、拖动停止生成、自动降档或减画质。
源时间候选只验未完成部分。详情见[实验索引](FG_EXPERIMENT_INDEX_2026-09-21.md)。

每个实现单独存档、固定基线；最小对照无收益就撤回，不用另一处优化遮住本项退步。
GPU 满载不是验收目标，功率稳定也不能替代运动/画面和帧间隔正确。
可消除的软件阻塞没有证据时，报告边界，不通过无限试验承诺硬件必能满倍率。

## 6. 证据与本轮验证

- [1.4.3 同条件对照](RELEASE_143_SCHEDULING_COMPARISON_2026-09-21.md)：本表历史版本数字。
- [1.4.3 发布说明](RELEASE_NOTES_1.4.3.md)：当时已公开的 6X/GC573/清晰度边界。
- [现有 XeSS 候选与失败记录](FG_STABILITY_PROGRESS_2026-09-21.md)。
- [NR/FG 跟进验收](NR_FG_FOLLOWUP_ACCEPTANCE_2026-09-21.md)。
- [修复总账](POST_1_4_3_REPAIR_LEDGER_2026-09-21.md)：历史截止点，包状态以当前入口为准。
- [5090 记录](RTX5090_CAPTURE_FG_REPAIR_2026-09-21.md)。
- [切换/YUY2/清晰度](FG_BACKEND_SWITCH_REPAIR_2026-09-19.md)。
- [采集音频/截图/字幕](CAPTURE_UI_SYNC_ACCEPTANCE_2026-09-19.md)。
- [七项解码/字幕审查](SEVEN_AUDIT_REPAIR_2026-09-19.md)。
- [UI/NR/源时钟纠偏](CORRECTIVE_AUDIT_2026-09-21.md)。
- [早期全链路验收边界](SCHEDULING_CHAIN_AUDIT_2026-09-18.md)。

本轮不跑 GPU/实卡、不重新编译、不打包、不提交/发布；仅核对代码与记录并更新文档。
v1.4.0 对照构建前阶段失败，不能说已跑出当前对 v1.4.0 的性能结论。
原有七个未提交代码/测试文件保留；HDR 候选仍待运行验收。
