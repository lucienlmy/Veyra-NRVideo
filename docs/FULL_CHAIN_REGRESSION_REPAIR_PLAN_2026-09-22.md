# 全链路卡顿、闪烁与采集清晰度复核方案

日期：2026-09-22。当前阶段：只读排查和方案，不实施新的产品修改。

基线：`edfd886`，分支 `codex/5090-capture-fg-20260921`，工作区
`E:/项目/Veyra/worktrees/playback-nr-20260920`。桌面 checkout 不是本轮开发基线。
本文件取代旧方案的执行顺序；历史文档继续作为证据，不自动转成待办。

范围补全：[全软件问题台账](WHOLE_PRODUCT_ISSUE_LEDGER_2026-09-22.md) 是完整问题入口，
单列历史 DLSS 6X、XeSS 4X 重负载倒退、低倍率不足及其他模块的遗留问题。
下文 A–E 是按边界组织的施工项，不表示必须先完成所有采集问题才处理历史补帧；
优先级按台账第 5 节，D1/D2/X1–X4/F1 同属首要任务。

## 1. 判断及边界

目前不能把所有反馈归为补帧预算过严，也不能把低 GPU 占用解释为必然有余量。
连续输出至少要求：输入持续到达、源画面被处理、时间历史匹配、生成图像有效、
图像按正确时间顺序呈现。这些环节中任意一处断档，都可能得到很高的平均 FPS
和很差的观感。不能只修 FPS 数字。

用户报告的“约 15 秒”只表示间歇性，不是固定周期。用户观察到欠速与闪烁相关，
这是关键复现条件，但还没有证明“丢帧必然导致亮度闪烁”。必须区分：

- 运动停顿/追赶：画面时间位置或输出节奏不连续。
- 纹理、亮度交替：历史恢复、原帧/生成帧颜色或图像内容不一致。
- 整屏色调变化：HDR 输出状态/交换链重建或系统显示路径变化。

本轮没有新增性能运行，也未复现用户的实际闪烁。不能宣称已找出唯一根因，
不能给成功概率保证。正式版、已有测试包、本地未提交候选分别记录。

## 2. 新日志能证明什么

用户日志：`C:/Users/123/Desktop/那天就是用着4k跑着的感觉挺清晰的，但突然屏幕卡住了，然后我重启了下软件，再打开发现画面就很糊了.log`。
RTX 5070 Ti；采集设备 VID 07ca/PID 2553，未核实具体型号。

**8 次处理图打开全部是 `sr=0 nr=0 fg=0 nvof=0`。这份日志中的卡顿不能归因于正在执行 NR 或补帧。**

| 位置/时间（日志 UTC） | 事实 | 不能据此推断 |
| --- | --- | --- |
| 行 370，13:13:43 | 输入 P010 3840×2160/30；处理/输出尺寸保持 4K | 分辨率计划里 NR=1080 不代表开启了 NR 或暗中缩图 |
| 行 484，13:13:56 | 源 311：PTS 跳 1187.52ms，回调到达间隔 1184.06ms | 不能区分驱动停供、前一次回调阻塞、线程失调度或应用锁竞争 |
| 行 497，13:13:58 | 源 330：PTS 跳 1314.37ms，回调到达间隔 1314.98ms | 不是只有显示线程漏报 FPS；也不是已证明采集卡硬件故障 |
| 行 496/509 | 恢复帧 Present CPU 约 0.40/0.43ms | 恢复帧快不代表断档期间没有应用阻塞 |
| 13:14:14 | 有序关闭 presenter/graph/device；14:08 再开 | 约 53 分钟空白不是已证明的死锁或自动重启 |

8 次实际输入按顺序是：NV12 1440p120、P010 1440p60、P010 4K30、
NV12 1440p120、NV12 4K60、NV12 720p60、NV12 1080p120、NV12 1920×1200/60。
日志有 UI 选择提交，不能把这些模式变化都称为软件静默降级。
4K30 前后没有新的应用启动记录；提供的文件无法还原用户所说的完整“崩溃后重启”过程。
文件结尾仍有正常运行统计，没有对应终止崩溃栈。

其他排除项：原生输入 ConnectDirect，没有系统解码/颜色转换器；P010 是未压缩输入，
不能称为“P010 解码太慢”。此设备没有嵌入音频 pin，音频恢复已停用，不能用反复音频重连
解释这段日志。驱动给出 10 个 allocator buffer 不等于已测得积压 10 帧。
断点日志有采样限频，回调耗时也不是全量记录，不能用后面没有警告证明断档不再发生。

### 与 5090 反馈的区别

此前 5090 日志是 4K P010/60、原生 NR、DLSS 2X。质量光流后处理接近源周期，
存在 mailbox 丢源帧和历史重置；1000 次预算判断约 999 接受/1 拒绝，
690 有效生成/0 无效，689 生成呈现/1 过期。不是大规模 admission 拒绝。
切换重建约 1.618 秒和约 95 帧丢失单列，不能混进稳态平均值。

`edfd886` 已接受的 P010/P016 冗余 CPU 上传消除，使本机 P010 中位值约
2.4ms 降至 0.93–0.98ms，18 个 GPU 输出指纹一致。这是独立优化，
不是证明上述断档、5090 整链路或闪烁已修好。

## 3. 全链路源码复核

以下位置基于当前工作区；“代码缺口”不等于“用户问题根因已证实”。

| 环节 | 证据/现状 | 结论 |
| --- | --- | --- |
| 采集模式身份 | `CapturePanel.cpp:114` 已按稳定 formatKey 恢复；`CaptureCardSource.cpp:484` URI 只带数字 format index；`AppShell.cpp:330,659` 原样保存/重开该 URI | 最近打开和初次打开的格式身份合同不完整；枚举顺序变化存在打开另一种模式的风险 |
| 实际协商核对 | `CaptureCardSource.cpp:565` 重新 GetStreamCaps(index)，SetFormat/GetFormat；没有与 UI 原选稳定格式键完整对照；`:1165` 重连比尺寸/颜色，但未比较默认模式 FPS | 应补齐请求与实际模式核对；不能重复发明 UI 已有的稳定键存储 |
| 回调/锁/I/O | `NativeCaptureSink.cpp:136` Receive 持有 sink 锁调用回调；`CaptureCardSource.cpp:386` 回调持源锁复制并可能写警告；`Log.cpp:95` 统一锁内格式诊断/写文件/flush | 同步阻塞风险成立，实际 1 秒断档归因未知；日志 flush 是 250ms/64KiB/警告触发，不能臆造 15 秒计时器 |
| 原始采集帧所有权 | mailbox 交换和复制在同一锁内；sink 检查动态媒体类型和样本大小 | 未发现此前“双帧覆盖”复发证据，不以重写采集架构作为方案 |
| 文件/压缩输入 | EAGAIN 重送、B 帧 PTS/metadata 对齐、关键帧恢复已有修复及局部验收 | 不重复修；压缩断流、真实卡队列仍需相关工况验证，不能把原生卡问题归因于压缩解码 |
| 读源与呈现 | `EngineController.cpp:947` 实卡 tryRead 不阻塞；同 owner 仍处理提交、呈现及管理调用 | 不存在“采集每次阻塞 read”这一现成结论；要定位确切等待边界 |
| 历史/重置 | `EnhanceGraph.cpp:1305,1361` PTS/场景分析可重置；`:1928` DLSS 跳过后播种；CPU 原始帧与 GPU 导入的亮度分析覆盖有差异 | 核对同一内容不同入口的断点判定及恢复内容；不能关闭所有 reset 来掩盖 |
| DLSS 恢复 | 当前无历史时一次 2X 播种，后续恢复用户倍率；v1.4.0 曾做整组 reset；已有 SDR 资源测试通过 | 不能把已接受的单次播种优化回退重试；欠速恢复后的真实图像/颜色一致性仍未验收 |
| XeSS 历史 | `VideoPresenter.cpp:171` 已比较 epoch、revision、motionPreviousSource；reset 时保留当前图作历史，非逐帧关闭 | 不能声称没有帧身份检查；需核验标记、纹理、provider 实际消费与提交身份一致 |
| 呈现资源 | 输出 lease/fence、双批上限已存在，SDR 早原帧/生成帧资源回归通过 | 完整 HDR/guidance/descriptor/provider 消费结束仍需按实际跨队列合同验证；未发现错误前不改队列结构 |
| HDR 状态 | 旧 `queryHdrDisplay` 把查询失败当 false，owner 周期查询可能触发 SDR 重建 | 确认存在状态区分缺口；工作区三态候选尚未运行验收，不能解释所有 SDR 闪烁 |
| 清晰度 | `EnhanceGraph.cpp:1569` 采集通常不启用 chroma reconstruction；`YuvToLinearRgb.hlsl:73` 最近邻取色度；`EngineController.cpp:292` 高质量显示采样仅 remote 开；`PresentBlit.hlsl:77` 其余双线性 | 可能解释彩边/非整数缩放观感差异，不证明整画面突然变糊；不能靠全局锐化或改颜色公式处理 |
| 界面/音频管理 | HDR 查询、设置事务、截图同步、音频状态/恢复都可能占用 owner；另有 UI 读取日志状态 | 与卡顿时间对齐后再定点移除阻塞；不盲拆 owner 或重做 UI |

## 4. 修复顺序与具体交付

### A. 先补采集身份合同，独立解决重开选错模式风险

让采集打开请求携带稳定设备路径和已选格式键，打开时重新解析匹配项，
最近打开复用相同合同。旧数字 URI 兼容解析，但不能凭不存在的键假装验证成功；
无法可靠还原时交回选择器确认。GetFormat/最终连接类型核对尺寸、格式、
默认或自定义帧周期，以及用户色彩覆盖，不擅自降分辨率或换格式。
重连沿用现有稳定键逻辑，补默认 FPS 核对；合法驱动帧周期舍入需容差，不能要求浮点相等。

验收：枚举重排、模式删除、驱动拒绝/改写请求、旧 URI、最近打开、断线重连；
明确区分用户改选与程序恢复。修改边界为 capture request/parser/store/open，不改 NGX。
这个缺口可以直接修，仍不得宣传为用户变糊已确认修复。

### B. 找到第一次断供的位置，再修卡顿放大链

复用现有 frame-flow、reset、callback、CPU stall 和 fence 信息，按异常前后关联：
采集回调进入/退出 -> mailbox -> 读源 -> 图提交/完成 -> 呈现进入/退出。
视频输入对应为 demux/decode -> 已解码帧 -> 后续公共链，不能只跑文件就宣布实卡通过。

只补缺失的关键边界：必要时用固定容量内存环保存异常前后片段，一次导出，
不用逐帧字符串/磁盘记录。CPU 长调用用短 ETW 等待栈区分被抢占、互斥锁、驱动调用、
磁盘写和 GPU 等待；现有约第 605 帧 NR Evaluate 30ms 是 CPU 墙钟观察，不是 NR GPU 耗时证明。

按证据采取对应修改：

- 若回调被应用锁/I/O拖住，移出锁内日志/管理操作；必要时使用有界日志写出，
  实时回调不等待磁盘。先用受控慢 writer 验证隔离，再测实际收益。
  历史“关逐帧日志仍卡”的反证保留，此项不重复作为通用 FPS 优化。
- 若 owner 被非呈现管理调用拖住，仅把该已确认调用改成有界状态查询/缓存交接，
  不移动整个 GPU/Present owner，不添加视频排队。
- 若 CPU 在运行而 GPU 依赖未完成，定位具体依赖/工作，不删除 fence，不用占用率判空闲。
- 若应用没有阻塞而驱动确实停供，保留真实断点，修恢复后的额外停顿；
  不伪造缺失输入，不以此推断其他机器都是硬件问题。

验收：在相同输入/效果下减少已归因的停顿，源覆盖与帧龄不退步；有意断供时
恢复时间和图像状态正确。不能只比较异常恢复后的快帧，必须包含整个断档。

### C. 两后端一起验欠速恢复的画面，先 2X

复现正常 -> 短时处理延迟/输入断点 -> 恢复，分别控制每个边界，避免同时改多个因素。
用确定运动轨迹及含静态灰块/色块的素材区分画面运动和亮度脉冲，
对照相同真实源帧在连续运行与中断恢复时的输出。像素抓取仅用于独立诊断运行。

DLSS 核对源对、子帧时间位置、有效状态、播种后第一完整组，以及原帧/生成帧
进入最终 SDR/HDR 输出时是否使用同一颜色合同。XeSS 核对 source identity、
motionPreviousSource、XeLL 周期、reset 与 provider 实际消费的纹理对应，
消费结束以前不得复用资源；不能把外层 Present 返回当成组内每帧完成。
NR/SR 也一起核对：重置前后残差、保护合成和 HDR 基底是否与当前源对一致。

只修改被测试击中的身份、资源退休或恢复状态错误；保留真实断点 reset，
不把“生成失败”“没有显示”“算法历史无效”混成同一种状态。
不撤销已验证的单次播种、不引入默认防闪滤镜掩盖错误、不靠锐化减少所谓果冻。
当前没有证据支持直接全盘重写算法模块。

有源断档时不能创造缺失的真实运动；目标是恢复正确且不额外闪烁/反复播种。
若画面内容正确但呈现仍突发，再处理确切的期限/已就绪帧错序问题。
没有错误拒绝证据时不改 admission；5090 当前记录尤其不支持放宽限制。

### D. 单独验收 HDR 查询失败候选

本轮开始前已有 `HdrDisplayState`/optional 查询未提交修改。区分 HDR、SDR、查询失败：
同显示目标查询失败不应伪装为用户关闭 HDR；新会话/显示目标变化必须重新建立状态，
不能无限沿用旧显示器状态。测试失败注入、明确关闭 HDR、切换显示目标和结束重开。
现有候选只构建未运行，不算完成；此轮不继续改代码。
若欠速 SDR 也闪，必须继续 C，不能用 D 结案。

### E. 采集清晰度独立验收

先确认实际输入模式与全关效果的 1:1 像素，核对步长、平面、裁切、range/matrix/transfer。
再用同模式、同窗口可视尺寸比较 PotPlayer；若两程序不能同时打开卡，采用可重复信号顺序采集。
不能拿 HDMI 直连或不同锐化/缩放档作为无损对照。

若损失发生在色度重建或最终缩放，复用项目已有正确采样实现，按色度位置/缩放比接入，
逐项测线条、彩色边缘、文字、HDR 数值和 GPU 成本。不全局抬锐度，不伪造 4:4:4 细节。
若原始 4K 样本已模糊而软件 1:1 正确，记录输入端差异，不能再加“软件清晰度修复”。

## 5. 明确不再安排的旧实验

已对照 [实验索引](FG_EXPERIMENT_INDEX_2026-09-21.md)、
[非 NR 回退](FG_NON_NR_EXPERIMENTS_2026-09-20.md)、
[恢复修复反证](DLSS_RECOVERY_REPAIR_2026-09-20.md)、
[修复总账](POST_1_4_3_REPAIR_LEDGER_2026-09-21.md)。以下没有进入本方案施工项：

- 全局取消 admission/无限接纳晚到 FG（204.10 -> 184.76 提交/s，丢源/过期更多）。
- 同组 GPU 成本估算器、取消呈现清屏、光流历史复制、原生光流尺寸直传、提高队列优先级。
- XeSS 缩成一组 pending（约 52.25 -> 49.48 源帧/s，另有启动问题）。
- NVOF/SR 重叠、简单拆 CPU/presenter/owner、逐输出发布，以及换名的 FG/Enhance 并行重叠。
- 扩队列、固定 20/35/50ms 等待、自动降倍率/减画质、拖动时停止生成。
- 把含 provider 等待的 Present 周期反馈给 XeSS；采集设置固定 2 秒超时。
- 取消全部 discontinuity/reset，或把已有 sticky driver flag 修复重新实现一次。

**已有候选不是新方向：** `VEYRA_TEST_XESS_SOURCE_TIMING` 默认关闭，有吞吐收益，
完整节奏/图像未验收；仅在 C 的输入身份和资源合同通过后完成候选验收，不重新包装为新优化。
SDK 允许 frameRenderTime=0，不能把当前默认值直接判为违规。

## 6. 验收及有限停止条件

先验证效果全关，再 DLSS 2X / XeSS 2X；追加 NR+FG、SR+NR+FG、HDR 组合，
效果顺序、原生/内部/输出尺寸和光流档固定。之后覆盖 DLSS 3/4/6X、XeSS 3/4X，
源 30/60fps 及文件 24fps；XeSS 没有 6X。文件、原生采集、压缩采集分别标记覆盖。
1.4.0 当前对照构建未成功，不能声称已做可运行版本对比；后续用能运行的固定基线，
版本差异缩小到出问题的合同，不把整个旧版替换当前修复。

先做最小 30 秒交错对照；有收益再 120 秒，单次不超过 300 秒。
比较全段源覆盖、有效生成、间隔 P95/P99/最大值、长空档/突发比例、同起止点帧龄、
像素/运动正确性、CPU/GPU 关键路径。同期监测本身开销单列；不同阶段 P95 不能相加。
GPU 利用率和功率只作辅助。应用/SDK 提交不等于系统显示，更不等于屏幕到人眼延迟。

满速工况沿用 [运行方案验收标准](FG_RUNTIME_REPAIR_PLAN_2026-09-21.md#7-验收标准)，
逐输出事件可观测时才计算组内节奏。欠速优化至少要求长间隔/P99改善超过基线波动，
源覆盖、有效生成、画质和帧龄不退步；不能通过丢更多原帧、多排一帧或重复画面达标。
正确性修复和性能收益分开，不能以单元测试通过宣称用户闪烁消失。

每项独立 checkpoint；一次最小实现，最多一次有新证据的修订，无收益或负优化则撤回
本项改动并记录。发现纯运行时/GPU关键路径超预算且没有已证实的可去除阻塞，就停止
该性能实验，报告可用边界，不自动降档，也不以 GPU 百分比低继续无限尝试。

相关回归：后端/倍率切换、seek、暂停/恢复、切镜、resize/跨屏、中键拖动、退出、
输入断开、HDR、字幕、截图；共享处理图变更加导出回归。没有实卡/第二屏就明确未测。
不把本机 RTX 5070 结果代替 30/40/5090 或反馈者实机结果。

## 7. 其他已知问题与范围

本节仅保留摘要，完整状态、证据和关闭条件见
[全软件已知问题台账](WHOLE_PRODUCT_ISSUE_LEDGER_2026-09-22.md)。
不能用这张简表替代历史 XeSS/DLSS 问题审查。

| 项目 | 当前状态/处理 |
| --- | --- |
| DLSS/XeSS 高低倍率欠速闪烁、间歇卡顿 | 本计划主线，根因未完整确认 |
| XeSS 果冻/功耗波动 | 身份/运动/节奏一起验证；没有证据证明锐化是共同原因 |
| 采集冻结、重开变糊、总体清晰度 | A/B/E；不要把三种症状强行归为一个问题 |
| GC573 RGB 低于标称、GC551 音频 | 历史修复未等于反馈者实卡解决；本轮无该卡新证据，不编造已解决 |
| HDR 浮窗色调变化、多屏/UI快速滚动 | 局部修复已保留，物理显示观感未全面验收；D 只覆盖查询失败，不包办全部 |
| 独立输入降噪、NR 防闪 | 独立降噪未交付；NR 残差防闪默认关闭，不作为补帧闪烁的替代修复 |
| XeSS/FSR 最终输出自定义限帧、Reflex+FG | 前者未支持；后者仍非完整原生 Reflex 链路，不冒充已完成 |
| 字幕布局/设置、导出、颜色预设等已修事项 | 保留已有验收，相关改动时回归；没有新证据不列为再次修复任务 |
| 杜比视界、NR 叠层、NVIDIA FSR4 | 前两项按用户决定暂缓，FSR4保持撤回 |

这是当前报告、源码和历史验收的合并复核，不是“全软件所有 bug 已穷尽”。

## 8. 本轮交付状态

仅新增本方案并更新 CURRENT_STATUS/WORKLOG 和旧方案入口提示；无新产品代码修改、
编译、GPU运行、打包、提交、合并或发布。已有未提交 HDR 候选和 SDR 测试改动保留。
后续产物按用途写入 `E:/项目/Veyra/{tests,logs,tmp,build}/full-chain-20260922/`，本轮尚未创建。

现有证据：[5090 上传优化和运行记录](RTX5090_CAPTURE_FG_REPAIR_2026-09-21.md)、
[七项审查验收](SEVEN_AUDIT_REPAIR_2026-09-19.md)、
[版本对照](RELEASE_143_SCHEDULING_COMPARISON_2026-09-21.md)。
