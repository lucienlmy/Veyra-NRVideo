# 补帧实验索引与重试约束

2026-09-21 核查，工作分支 codex/fg-stability-20260921。
本文件是继续性能实验前的入口；失败只针对已测实现和工况，不证明整个技术方向永远无效。

| 实验 | 当前代码状态 | 结论与证据 |
| --- | --- | --- |
| 同组 GPU 成本替代独立 P95 相加 | 临时入口已删除 | 无可重复收益，见下方非 NR 实验记录。不要再次把这个估算器作为未经尝试的新方案 |
| 去掉呈现清屏 | 临时入口已删除 | 已回退，详见非 NR 实验记录 |
| 光流历史复制 | 临时入口已删除 | 已回退，详见非 NR 实验记录 |
| 原生光流尺寸直接供 DLSSG | 临时入口已删除 | 已回退，详见非 NR 实验记录 |
| 提升图形队列优先级 | 临时入口已删除 | 未取得可重复改善，已回退 |
| XeSS 只保留一组待处理源帧 | 两行实验代码已删除，恢复版已构建 | 初版阻塞启动；豁免启动后吞吐约52.25降至49.48源帧/秒，虽帧龄/突发改善仍不接受 |
| NVOF 与 Video SR 重叠 | 可执行入口已删除 | 未接受的历史候选；仅保留失败结论，避免重复尝试 |
| XeSS 真实源时间提示 | 代码仍在，VEYRA_TEST_XESS_SOURCE_TIMING 默认关闭 | 吞吐改善有重复证据，完整节奏、图像对应验收未完成，不能称修复完成 |
| XeSS fence / deadline 只读诊断 | 保留，追踪按诊断开关启用 | 用于区分依赖等待与截止时间等待，不是调度优化 |
| XeSS 文件播放 XeLL `bLowLatencyMode=0`（2026-09-22 X2） | 已撤回，仅 `VEYRA_TEST_XESS_LOW_LATENCY=0` 诊断开关 | 提供方 `SetEnabled` 返回 -15，补帧被拒；Intel 指南要求 XeLL 启用。不要再试 |
| XeSS/FSR 提供方 Present 移到辅助线程（2026-09-22 X3，单飞、fence 合同不变） | 代码已删除 | 真超分+NR+4X 源 51.0→23.8/s，GPU 95→44%，提供方周期估计 15.9→26.3 ms，组内 4.3→9.9 ms。owner 阻塞恰好锁定了源提交与提供方节奏；不改写提供方周期估计就解耦线程是负优化 |
| XeSS 真实源 PTS 间隔作 frameRenderTime（X1，已转正） | 默认开启，`VEYRA_DISABLE_XESS_SOURCE_TIMING=1` 对照 | 组内间隔 4.96→4.27 ms，Present 阻塞 p50 17.2→15.0 ms，源率不变 |
| 有界预览跳帧保留历史（F2） | 默认开启，`VEYRA_TEST_PREVIEW_SKIP_RESET=1` 对照 | 重置 177→1，原帧间隔 p95 26.8→22.2 ms，源率 53.5→50.8/s；肉眼待验收 |
| DLSS 超预算对改 2X 组（F3） | 默认开启，`VEYRA_TEST_FG_NO_REDUCED=1` 对照 | 见执行记录 §4.2 |
| 全局取消 DLSS admission / 无限制接纳晚到生成 | 可执行 A/B 入口已删除 | 既有采集测试约204.10降至184.76次提交/秒、过期和输入丢失增加；文件测试也严重回退，正式路径固定使用 admission |

核查 src/include/tests 未发现前六项临时环境变量入口。
默认关闭不等于已删除；未验收候选不得悄悄默认开启或计入发布修复。

## 2026-09-22 补充：内容位置误差不是新调度路线

有界验收的矩形平移诊断再次出现 6X 位置误差，解析运动向量对照没有消除；
这与 [09-17 原始记录](POST_1_4_0_FIELD_FAILURE_REPAIR_PLAN_2026-09-17.md)
中方块/正弦失败、非周期细节通过的内容依赖相符。
不重复尝试反向/零/精确 motion、相机矩阵、FrameID 类型、串行等待等旧对照，
也不把诊断失败当作已证明运行库或调度器是唯一根因。
本次未为此改变产品参数。新增显式诊断保留原门槛及失败退出码，
证据见 [有界执行记录](BOUNDED_REPAIR_EXECUTION_2026-09-22.md)。

## 继续工作前必须核对

1. 查本索引及原始实验记录，包括固定等待、盲目拆队列等更早的失败方向。
2. 若重试已失败方向，先写出新证据、与旧实现的实质区别、成功门槛；换参数不算新证据。
3. 单个假设先一次最小实验，失败后最多一次有证据的修订。继续失败就撤回本项修改。
4. 同时比较源帧覆盖、间隔尾部、帧龄、画质和功能；不能仅凭平均 FPS 接受。
5. 记录源码存档、输出目录、对照结果和删除状态。不得把保留的诊断统计当优化收益。

## 原始证据

- [独立修复执行记录 2026-09-22](FG_INDEPENDENT_REPAIR_EXECUTION_2026-09-22.md)
- [独立复核与方案 2026-09-22](FG_INDEPENDENT_REVIEW_2026-09-22.md)

- [非 NR 实验与回退](FG_NON_NR_EXPERIMENTS_2026-09-20.md)
- [本轮 XeSS 实验、候选与逐次证据](FG_STABILITY_PROGRESS_2026-09-21.md)
- [执行方案及有限实验要求](FG_STABILITY_COMPLETION_PLAN_2026-09-21.md)
- [当前链路复核](FG_PIPELINE_REASSESSMENT_2026-09-21.md)
- [新的有界重构方案](FG_RUNTIME_REPAIR_PLAN_2026-09-21.md)
- [取消 admission 的采集反证](DLSS_RECOVERY_REPAIR_2026-09-20.md)
- [取消 admission 的文件反证与串行预算](FG_CADENCE_REPAIR_ACCEPTANCE_2026-09-20.md)
- [工作记录](WORKLOG.md)

开工存档 d2ae8ee；最新一组排队回退记录 53490a6。
本次索引更新不改产品代码、不构建、不运行新性能测试。
